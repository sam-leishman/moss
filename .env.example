# Local Development Configuration
# Copy this to .env for local development

PORT=3000
HOST=localhost

# Logging configuration
# LOG_LEVEL: Minimum log level to output (DEBUG, INFO, WARN, ERROR, FATAL)
# Default: DEBUG in development, INFO in production
LOG_LEVEL=DEBUG

# Local paths for development (adjust to your local setup)
# MEDIA_PATH: Root directory for media libraries (default: ./test-media in dev, /media in production)
# CONFIG_DIR: Directory for database and configuration (default: ./test-config in dev, /config in production)
# METADATA_DIR: Directory for generated thumbnails and cached data (default: ./test-metadata in dev, /metadata in production)
MEDIA_PATH=./test-media
CONFIG_DIR=./test-config
METADATA_DIR=./test-metadata
